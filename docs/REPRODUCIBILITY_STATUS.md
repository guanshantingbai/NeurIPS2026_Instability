# Reproducibility Status (post fast-path honesty fix)

**Latest end-to-end validation (clean `outputs/`, no guessed paths):** **`docs/END_TO_END_VALIDATION.md`** (2026-05-12). **PromptAD pre-search follow-up:** an earlier repo-wide search found **no** pre-existing `CLS-*-per_sample.csv` trees (see **`docs/PROMPTAD_RESULT_RECOVERY.md`** and **`docs/END_TO_END_VALIDATION.md`** §9). **PromptAD minimal full-path (same day, post-search):** **`minimal full-path verified`** — **not** the full **81-setting** paper reproduction. Configuration: **`mvtec` / `bottle` / shot `1` / seeds `111,222`**; data **`/home/zju/datasets/mvtec`** (matches upstream `~/datasets/mvtec` here); **`PROMPTAD_OUTPUT_ROOT=$REPO_ROOT/outputs/promptad_fullpath_minimal`**; short train via **`PROMPTAD_TRAIN_EXTRA_ARGS="--Epoch 3 --batch-size 8 --eval-freq 1 --num-workers 2"`**; **`PROMPTAD_GPU=0`**. Unified raw → Sec 3 / Sec 4 with **`SEC3_PROMPTAD_MAX_PAIRS_PER_SETTING=200000`**, **`SEC4_MAX_PAIRS_PER_SETTING=200000`**; **`bash scripts/reproduce_main.sh`** → **exit 0**. Details: **`docs/END_TO_END_VALIDATION.md`** §11, **`docs/FULLPATH_PROMPTAD.md`** section **(C)**. Large pairwise runs: `run.sh` honors **`SEC3_PROMPTAD_MAX_PAIRS_PER_SETTING`** / **`SEC4_MAX_PAIRS_PER_SETTING`** (and optional `*_PAIR_SAMPLING_SEED` vars); subsampling is reflected in summary JSON when used.

Earlier verification note: **2026-05-11** after wiring `PYTHONPATH` via `scripts/repo_env.sh`, removing masking `|| true`, and making fast vs `FULL_RUN=1` explicit.

Commands were run from repo root in this order (after `bash scripts/clean_outputs.sh` once):

1. `bash scripts/reproduce_sec3_promptad.sh`
2. `bash scripts/reproduce_sec3_padim.sh`
3. `bash scripts/reproduce_sec4_systematic.sh`
4. `bash scripts/reproduce_main.sh`
5. `bash scripts/reproduce_app_patchcore_tta.sh`
6. `bash scripts/reproduce_appendix.sh`

## Per-command results (this verification)

| # | Command | Exit | Generated outputs | Mode |
|---|---------|------|-------------------|------|
| 1 | `reproduce_sec3_promptad.sh` | **0** with raw or stub flag; **non-zero** if raw missing and `SEC3_PROMPTAD_ALLOW_STUB` unset | From raw: **pairwise-definition** tables (`pairwise_metrics.csv`, `setting_level_metrics.csv`, `sample_level_metrics.csv`, `same_auroc_instability_pairs.csv`, `risk_coverage.csv`, summary JSON) + PNGs under `outputs/figures/sec3_promptad/`; stub path copies `pairwise_stub.csv` only | **raw-derived** or **stub** |
| 2 | `reproduce_sec3_padim.sh` | **0** | `outputs/cached_results/sec3_padim/marginal_stub.csv`, `outputs/figures/sec3_padim/fastpath_done.txt` | **cached-only** |
| 3 | `reproduce_sec4_systematic.sh` | **0** | **If PromptAD unified raw exists:** `outputs/cached_results/sec4_systematic/*.csv`, `sec4_systematic_from_raw_summary.json`, PNGs + `from_promptad_raw_done.txt` under `outputs/figures/sec4_systematic/` (`analyze_sec4_promptad_from_raw.py`). **Else:** `fastpath_done.txt` + stub `compute_*.py` stdout only | **raw-derived (pairwise Sec 4)** or **stub** |
| 4 | `reproduce_main.sh` | **0** only if step 1 succeeds (raw or stub); **non-zero** if PromptAD raw missing and stub unset | Same as 1–3 in intent; **fails fast** when Sec 3.1.1 cannot run | **mixed** — see `docs/END_TO_END_VALIDATION.md` |
| 5 | `reproduce_app_patchcore_tta.sh` | **0** | `outputs/cached_results/app_patchcore_tta/*.csv`, `outputs/figures/app_patchcore_tta/*.png|*.pdf` | **cached-only** (copy from `result_analysis/patchcore_tta/`) |
| 6 | `reproduce_appendix.sh` | **0** | Appendix C/E tables + markers; F as in row 5; G CSV+PNG+PDF under `outputs/figures/app_signal_comparison/` and CSV mirror under `outputs/tables/app_signal_comparison/` | **cached-only / stub** (Appendix E: bundled `mechanism_stub.csv` only — **not** the PaDiM seed-killer pipeline) |

**Alignment with `docs/FIGURE_MAP.md`:** The map now lists the **actual fast-path paths** above. It does **not** claim PDF paper figures for Sec 3–4 are regenerated by default.

## Classification

### Fully reproducible (paper-identical, from raw data)

- **None** in default fast path. Requires `FULL_RUN=1`, datasets, checkpoints, and long GPU jobs per upstream code.

### Reproducible from cached / bundled lightweight artifacts

- **Section 3.1.1 PromptAD:** **raw-derived** when `outputs/cached_results/raw_scores/promptad/unified_raw_scores_wide.csv` (or `_long.csv`) exists — `analyze_sec3_promptad_from_raw.py` builds **pairwise** tables/figures (anomaly–normal pairs, \(z_{\mathrm{sem}}/z_{\mathrm{vis}}/z_{\mathrm{fused}}\), \(\mathrm{Var}([z_{\mathrm{sem}},z_{\mathrm{vis}}])\), no semantic–visual score-difference instability proxy). **Stub:** `SEC3_PROMPTAD_ALLOW_STUB=1` copies bundled `pairwise_stub.csv` + marker (no empirical raw).
- **PromptAD Stage 1 minimal smoke (2026-05-12):** **`minimal full-path verified`** on this host — `train,infer,export` for **`mvtec` / `bottle` / shot `1` / seeds `111,222`** (short `--Epoch 3`), then unified raw + pairwise Sec 3 / Sec 4 + `reproduce_main.sh` (**not** the full **81-setting** paper grid). See **`docs/FULLPATH_PROMPTAD.md`** section **(C)** and **`docs/END_TO_END_VALIDATION.md`** §11.
- **Section 4 systematic (same raw gate):** `src/experiments/sec4_systematic_validation/run.sh` runs **`analyze_sec4_promptad_from_raw.py`** when the same unified raw files exist — pairwise Sec 4-style tables/figures under `outputs/cached_results/sec4_systematic/` and `outputs/figures/sec4_systematic/` without calling `external/PromptAD` utilities.
- **Section 3.1.2 PaDiM (fast path):** bundled `marginal_stub.csv` + `fastpath_done.txt`.
- **Appendix F PatchCore + TTA (fast path):** copies from `result_analysis/patchcore_tta/` (fails with **non-zero** exit if that directory or any `*.csv` is missing).
- **Appendix G (fast path):** runs `external/PromptAD/utils/supplementary_signal_baselines.py` on **bundled** stub trees under `src/experiments/app_signal_comparison/samples/fastpath/` (real script, toy data).

### Partial / structural only

- **Section 4 (stub fast path, no unified raw):** local Python stubs run and write `fastpath_done.txt` only; **does not** reproduce Fig 3–6 from the PDF. **`FULL_RUN=1`** on `run.sh` (only reached when unified raw is **missing**) still invokes `promptad_strengthening_experiments.py` via the adapter (heavy; needs pilot CSVs under `external/PromptAD/...`).
- **Section 4 (from PromptAD unified raw):** when `unified_raw_scores_{wide,long}.csv` is present, `run.sh` exits **before** stubs and runs **`analyze_sec4_promptad_from_raw.py`** only — pairwise metrics, near-AUROC seed pairs (default \(\epsilon=0.002\)), controlled margin / failure-conditioned signals / instability-regime failure rate / decision-consequence delta risk — **no** PromptAD train/infer and **no** `external/PromptAD` analysis scripts.

### Placeholder / not on default path

- `src/experiments/sec3_promptad_observation/compute_promptad_pairwise.py` — **not invoked** by `run.sh` (reserved).
- `src/experiments/sec3_padim_observation/compute_padim_marginal_views.py` — **not invoked**; exits with message if run directly.

### Missing data or external dependency (full rerun)

- **Datasets** (MVTec / VisA, etc.) for any `FULL_RUN` PatchCore / PromptAD / PaDiM job.
- **`FULL_RUN=1` + PatchCore Stage 1:** `scripts/run_patchcore_raw.sh` only (scores → `outputs/cached_results/raw_scores/patchcore/` + unified CSVs). **`reproduce_app_patchcore_tta.sh` is Stage 2** and never calls it; with existing `unified_raw_scores_long.csv`, Stage 2 runs **analyze only** (still needs `PATCHCORE_DATA_ROOT` / `PATCHCORE_MODELS_RUN` for upstream argparse). Default fast path copies from `result_analysis/patchcore_tta/` when raw is absent.
- **`FULL_RUN=1` + Appendix C:** must pass valid subcommands/args to upstream CLIs (e.g. `phase3 --proxy u6`); invoking with **no** args is expected to **fail** with usage error.
- **PaDiM Stage 1:** `FULL_RUN=1 bash scripts/run_padim_raw.sh` with `PADIM_*` (see **`docs/FULLPATH_PADIM.md`**). **Stage 2** `reproduce_sec3_padim.sh` / `reproduce_app_padim_representation.sh` **never** invoke it; they consume `marginal_protocol_b.csv`, `mechanism_from_raw.csv`, or bundled stubs.
- **PromptAD Stage 1:** `FULL_RUN=1 bash scripts/run_promptad_raw.sh` (see **`docs/FULLPATH_PROMPTAD.md`**). **`reproduce_*` does not call it.** **Export-only** builds unified raw from existing `CLS-*-per_sample.csv` trees. **Train/infer** use upstream fixed dataset paths (`~/datasets/mvtec`, etc.) unless symlinked. **Minimal train+infer+export** verified on **2026-05-12** for **`mvtec` / `bottle` / `k=1` / seeds `111,222`** (see doc section **(C)**); **not** full-grid parity.
- **`FULL_RUN=1` + Appendix G:** set `PROMPTAD_ROOT` / `PILOT_DIR_OVERRIDE` if not using defaults under `external/PromptAD/`.

## Engineering fixes applied (summary)

- `scripts/repo_env.sh` + `export PYTHONPATH="$REPO_ROOT:..."` sourced from every `scripts/*.sh` and `src/experiments/*/run.sh`.
- Removed `|| true` masking; subprocess failures propagate.
- Default paths in `supplementary_signal_baselines.py` use repo-relative defaults; bundled stub pilot under `src/experiments/app_signal_comparison/samples/fastpath/`.
- PaDiM `padim_seed_killer_evidence_pipeline.py` and `padim_seed_killer_appendix_pair_figures.py` use `external/PromptAD` for shared imports.
- Appendix F: **Stage 1** `run_patchcore_raw.sh`; **Stage 2** `reproduce_app_patchcore_tta.sh` (analyze from cached raw or fast path copy from `result_analysis/patchcore_tta/` — see `docs/MODEL_REPRODUCTION.md`).

## PatchCore (Stage 1 + Stage 2; not end-to-end verified in-repo unless本机跑通)

- **Stage 1:** `FULL_RUN=1 bash scripts/run_patchcore_raw.sh` (scoring + unified export; **not** called by `reproduce_*`).
- **Stage 2:** `bash scripts/reproduce_app_patchcore_tta.sh` — if `unified_raw_scores_long.csv` exists, **analyze only**; else fast path from `result_analysis/patchcore_tta/`. **`FULL_RUN=1` does not trigger Stage 1** from `reproduce_*`.
- **Documentation:** `docs/FULLPATH_PATCHCORE.md`.
- **Raw + unified outputs (Stage 1):** `outputs/cached_results/raw_scores/patchcore/`.
- **Status / blocker:** **partial** — wiring is **raw path + clear failure modes**; **not** CI end-to-end GPU-verified. Local success needs valid data, checkpoints, CUDA, FAISS.

## PaDiM (Stage 1 + Stage 2; interface validation passed; **not** end-to-end GPU-verified in-repo)

- **Stage 1:** `FULL_RUN=1 bash scripts/run_padim_raw.sh` (**not** invoked by `reproduce_*`).
- **Stage 2:** `bash scripts/reproduce_sec3_padim.sh`, `bash scripts/reproduce_app_padim_representation.sh` — consume `marginal_protocol_b.csv`, `mechanism_from_raw.csv`, or bundled stubs only. **`FULL_RUN=1` does not trigger Stage 1** from `reproduce_*`.
- **Documentation:** `docs/FULLPATH_PADIM.md`.
- **Raw scores (Stage 1):** `outputs/cached_results/raw_scores/padim/` (`unified_raw_scores_long.csv`, `unified_raw_scores_wide.csv`, `manifest.json`).
- **Interface checks (no full GPU/data):** `bash -n` passes; `run_padim_raw.sh` fails clearly on bad env / bad `PADIM_DATA_ROOT`; failed Stage 1 does not write unified CSVs; minimal fake jobs + aggregate + plot **consume** downstream formats correctly.
- **Appendix E:** **partial** — Stage 2 uses **minimal** `mechanism_from_raw.csv` when present; **not** full seed-killer evidence (see **`docs/FULLPATH_PADIM.md`**).

## PromptAD Stage 1 (raw evidence; **minimal full-path verified** on 2026-05-12 — not full 81-setting grid)

- **Script:** `FULL_RUN=1 bash scripts/run_promptad_raw.sh` (requires `PROMPTAD_OUTPUT_ROOT`; `PROMPTAD_DATA_ROOT` when mode includes **train** or **infer**).
- **Outputs:** `outputs/cached_results/raw_scores/promptad/unified_raw_scores_{wide,long}.csv` + `manifest.json`; per-sample trees under `PROMPTAD_OUTPUT_ROOT` (e.g. `outputs/promptad_fullpath_minimal/` — **gitignored**).
- **Section 3.1.1 consumption:** `src/experiments/sec3_promptad_observation/run.sh` reads unified files (no PromptAD train/infer in Stage 2) and runs **pairwise** aggregation. Missing raw → error; optional **`SEC3_PROMPTAD_ALLOW_STUB=1`** for stub only.
- **Status:** **Minimal closed loop verified** — `mvtec` / `bottle` / `k=1` / seeds **`111,222`**, short `Epoch`, GPU `0`, data at **`/home/zju/datasets/mvtec`** (same as `~/datasets/mvtec` here). **Not** a bundled full `result_round1` in-repo; **not** paper-wide 81-setting reproduction. **Appendix C/G** remain separate paths (`docs/FIGURE_MAP.md`).

## Remaining blockers (honest list)

- No default script regenerates **camera-ready paper figures** for Sec 3–4; that remains **full-run-required**.
- Other files under `external/` still contain legacy `/PromptAD` strings in unrelated utilities; only paths required for the **default fast path** and the above PaDiM bridges were updated in this pass.
