# External Model Code

This directory hosts external model repositories used by adapters:

- `PromptAD/`
- `PaDiM-Anomaly-Detection-Localization-master/`
- `patchcore-inspection/`

Core paper logic should remain in `src/core` and `src/experiments`.
Avoid editing external code unless strictly required.
